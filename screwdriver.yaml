shared:
    image: node:8

parameters:
  p1: p1

jobs:
    main:
        template: sd2/noop
        requires: [~pr, ~commit]
        steps:
        - export: export GREETING="Hello, world!"
        - hello: echo $GREETING
